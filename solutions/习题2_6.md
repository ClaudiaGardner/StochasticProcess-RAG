# 习题2.6

## 题目

第2章第6题（PDF解析不完整，请参考原文）

## 解答

**Revisiting Strategy**

Now I'm revisiting the potential sources to ensure precision, focusing on the common book used in China. I am weighing the core problem and considering other classic issues from similar problems in chapter 2 of stochastic processes. I will start by identifying the most frequently asked problems in Chinese textbooks and related online forums. The approach is to provide a comprehensive and accurate answer.


鉴于你未提供具体的题目文本，根据“习题 2.6”以及“概率论与随机过程”这一学科背景，这通常指的是经典教材（如 Sheldon M. Ross 的《Stochastic Processes》或国内通用教材）中关于**泊松过程（Poisson Process）**的一个经典问题。

最常见的习题 2.6 是：
**设 $\{N(t), t \ge 0\}$ 是参数为 $\lambda$ 的泊松过程，试求自相关函数 $E[N(t)N(t+s)]$，其中 $s \ge 0$。**

以下是该题的详细解答。

---

### 题目分析

本题考查**泊松过程（Poisson Process）**的核心性质，特别是**独立增量性（Independent Increments）**和**平稳增量性（Stationary Increments）**。

*   **核心概念**：
    1.  泊松过程 $N(t)$ 服从参数为 $\lambda t$ 的泊松分布。
    2.  对于 $t_1 < t_2$，增量 $N(t_2) - N(t_1)$ 与 $N(t_1)$ 相互独立。
    3.  增量 $N(t_2) - N(t_1)$ 服从参数为 $\lambda(t_2 - t_1)$ 的泊松分布。
*   **求解目标**：计算两个不同时刻状态乘积的数学期望 $E[N(t)N(t+s)]$。

### 解题过程

设 $s \ge 0$。我们需要计算 $E[N(t)N(t+s)]$。

**第一步：利用独立增量性拆分变量**

由于 $N(t)$ 和 $N(t+s)$ 不是独立的（因为时间区间重叠），我们不能直接拆分期望。我们需要构造独立的增量。
将 $N(t+s)$ 写为 $N(t)$ 与增量之和：
$$N(t+s) = N(t) + [N(t+s) - N(t)]$$

**第二步：代入期望公式并展开**

将上述表达式代入待求期望中：
$$
\begin{aligned}
E[N(t)N(t+s)] &= E\left[ N(t) \left( N(t) + [N(t+s) - N(t)] \right) \right] \\
&= E\left[ N(t)^2 + N(t)[N(t+s) - N(t)] \right]
\end{aligned}
$$

利用数学期望的线性性质（$E[X+Y] = E[X] + E[Y]$）：
$$E[N(t)N(t+s)] = E[N(t)^2] + E\left[ N(t)[N(t+s) - N(t)] \right]$$

**第三步：利用独立性计算第二项**

根据泊松过程的定义，增量 $N(t+s) - N(t)$ 发生在时间区间 $(t, t+s]$，而 $N(t)$ 发生在时间区间 $[0, t]$。
由于这两个时间区间不重叠，因此随机变量 $N(t)$ 与增量 $[N(t+s) - N(t)]$ 是**相互独立**的。

对于独立随机变量 $X, Y$，有 $E[XY] = E[X]E[Y]$。因此：
$$E\left[ N(t)[N(t+s) - N(t)] \right] = E[N(t)] \cdot E[N(t+s) - N(t)]$$

**第四步：计算各项的矩**

已知 $N(t) \sim P(\lambda t)$，且增量 $N(t+s) - N(t) \sim P(\lambda s)$。

1.  **计算 $E[N(t)]$ 和 $E[N(t+s) - N(t)]$**：
    对于泊松分布 $X \sim P(\mu)$，期望 $E[X] = \mu$。
    $$E[N(t)] = \lambda t$$
    $$E[N(t+s) - N(t)] = \lambda s$$

2.  **计算 $E[N(t)^2]$**：
    对于泊松分布 $X \sim P(\mu)$，方差 $Var(X) = \mu$。
    利用公式 $Var(X) = E[X^2] - (E[X])^2$，可得 $E[X^2] = Var(X) + (E[X])^2 = \mu + \mu^2$。
    此处 $\mu = \lambda t$，故：
    $$E[N(t)^2] = \lambda t + (\lambda t)^2$$

**第五步：综合结果**

将第四步计算的结果代回第三步的方程中：
$$
\begin{aligned}
E[N(t)N(t+s)] &= (\lambda t + \lambda^2 t^2) + (\lambda t) \cdot (\lambda s) \\
&= \lambda t + \lambda^2 t^2 + \lambda^2 ts \\
&= \lambda t + \lambda^2 t (t + s)
\end{aligned}
$$

### 答案

$$E[N(t)N(t+s)] = \lambda t + \lambda^2 t(t+s), \quad (s \ge 0)$$

### 知识延伸

1.  **协方差计算**：
    本题的结果常用于计算泊松过程的协方差函数 $Cov(N(t), N(t+s))$。
    $$
    \begin{aligned}
    Cov(N(t), N(t+s)) &= E[N(t)N(t+s)] - E[N(t)]E[N(t+s)] \\
    &= [\lambda t + \lambda^2 t(t+s)] - (\lambda t)[\lambda(t+s)] \\
    &= \lambda t
    \end{aligned}
    $$
    更一般地，对于任意 $t_1, t_2$，协方差为 $Cov(N(t_1), N(t_2)) = \lambda \min(t_1, t_2)$。

2.  **解题技巧**：
    处理随机过程（尤其是独立增量过程如泊松过程、维纳过程）的乘积期望 $E[X(t_1)X(t_2)]$ 时，**“构造增量法”**是标准技巧。即假设 $t_1 < t_2$，将 $X(t_2)$ 写成 $X(t_1) + [X(t_2)-X(t_1)]$，利用独立性化简。

3.  **易错点**：
    直接假设 $E[N(t)N(t+s)] = E[N(t)]E[N(t+s)]$ 是错误的，因为这两个变量在时间上是相关的（重叠了 $[0, t]$ 区间）。必须先分离出独立的增量部分。

---
